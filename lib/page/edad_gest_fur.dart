import 'package:flutter/material.dart';

class Edadgestacionalfur extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xff800040),
        title: Text('Edad gestacional según FUR'),
      ),
    );
  }
}
